define([
	"jquery", 
	"backbone",
	"api",
], function($, Backbone, api) {

	var LanguageModel = Backbone.Model.extend({
		
        // Model Constructor
        initialize: function() {
		
        },
		
	});

	return LanguageModel;
});